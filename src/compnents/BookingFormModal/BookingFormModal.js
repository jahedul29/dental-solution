// import React, { useState } from "react";
// import { useParams } from "react-router-dom";
// import availableAppointments from "../../fakeData/appointments";

// const BookingFormModal = () => {

//   const { availableAppointmentId } = useParams();

//   const selectedAvailableAppointment = availableAppointments.find(
//     (appointment) => appointment.id === parseInt(availableAppointmentId)
//   );

//   const { title } = selectedAvailableAppointment;

//   return (

//   );
// };

// export default BookingFormModal;
